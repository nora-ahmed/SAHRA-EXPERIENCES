<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Confirmed_Requires_Quoted_Price</fullName>
    <active>true</active>
    <description>Prevents progression to Confirmed status until a Quoted Price is set.</description>
    <errorConditionFormula>
        AND(
            ISPICKVAL(Status__c, 'Confirmed'),
            ISCHANGED(Status__c),
            ISBLANK(Quoted_Price__c)
        )
    </errorConditionFormula>
    <errorMessage>The Event cannot be set to Confirmed until a Quoted Price is specified.</errorMessage>
</ValidationRule>